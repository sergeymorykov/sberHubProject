import styled from '@emotion/styled';
import Box from '@mui/material/Box';

export const BoxStyled = styled(Box)`
  text-align: center;
  margin-bottom: 16px;
  padding: 8px 16px;
  border: 2px solid #007bff;
  border-radius: 8px;
  background-color: rgb(from var(--tg-theme-bg-color) r g b / 50%);
  box-shadow: 0 4px 20px rgb(from var(--tg-theme-text-color) r g b / 20%);
`;
