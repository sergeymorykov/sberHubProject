import styled from '@emotion/styled';

export const TitleStyled = styled.h1`
    font-size: 28px;
    text-align: center;
    text-color: var(--tg-theme-button-text-color);
    color: var(--tg-theme-text-color);
`;