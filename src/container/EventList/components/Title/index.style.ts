import styled from '@emotion/styled';

export const TitleStyled = styled.div`
  color: var(--tg-theme-text-color);
  font-size: 1.5rem;
  font-weight: bold;
  margin-bottom: 24px;
  width: 100%;
  text-align: center;
`;
